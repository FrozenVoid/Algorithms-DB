Process:
A file of N bytes is split
into K-size chunks(end zero-padded).
A random K-size chunk 
is generated by PRNG seed,
and is Xored with each K-size
 chunk, then various entropy
 stats are measured and if
entropy is below original,
the PRNG seed is recorded as
new record
