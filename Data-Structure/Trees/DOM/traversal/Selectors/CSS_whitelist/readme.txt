CSS whitelist is the technique when you
have a website with lots of (possibly dynamically) generated advertising and similar trash.
CSS for such website would first block everything,
and whitelist the valid data selectors one by one.

